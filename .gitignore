# Secrets — never commit a real API key
.env
.env.*
*.key

# Internal working notes — not for the public repo
distribution-notes.txt

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Node (only if examples grow to include scripts)
node_modules/
